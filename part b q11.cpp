#include <iostream>
using namespace std;

int main() {
    float a, b, c, total, per;
    cin >> a >> b >> c;
    total = a + b + c;
    per = total / 3;
    cout << total << endl << per;
    return 0;
}
